export const metadata = {
  title: "Playground — freeroute",
  description: "Unified AI Chat & Web Designer Studio with live preview, code inspection, and telemetry.",
};

export default function PlaygroundLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
