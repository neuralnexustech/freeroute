DATABASE_URL="file:~/.freeroute/freeroute.db"
PORT=20128
JWT_SECRET="change-me-in-production-generate-with-openssl-rand-base64-48"
API_KEY_SECRET="change-me-generate-with-openssl-rand-hex-32"
INITIAL_ADMIN_EMAIL="admin@experientiallabs.ai"
# 64 hex chars (32 bytes). Encrypts key secrets for dashboard reveal. Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
FREEROUTE_MASTER_KEY="change-me-generate-64-hex-chars"
